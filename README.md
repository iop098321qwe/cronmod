# Cronmod

Simple helpers for working with user crontab entries.
Provides quick listing and editing with a post-edit mirror script.

## Functions
- `crone`: Edit crontab, then run `~/.local/bin/cron_mirror.sh`.

## Aliases
- `cronl`: List crontab entries.
